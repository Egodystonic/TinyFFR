<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <Platforms>x64;ARM64</Platforms>
    <NoWarn>
      CS7022; <!-- Fixes an error where MSTest generates an entry point in this library because we're referencing the unit test project -->
    </NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\TestCommon\TestCommon.csproj" />
    <ProjectReference Include="..\TinyFFR.Tests\TinyFFR.Tests.csproj" />
  </ItemGroup>

</Project>
